
const read=require('readline-sync');

let arr=read.question("enter array\n");
let number=read.question("enter number\n");
arr=arr.split(",");

let indexer=(arr,number)=>{
  return arr.indexOf(number);
}

indexer(arr,number);