
const read=require("readline-sync");

let str=read.question("enter string\n");

let length=(str)=>{
  
  let lengthvalue=0;
  str=str.split('').map(item=>{lengthvalue++;return item})
  return lengthvalue>7?"true":"false";
}
length(str);